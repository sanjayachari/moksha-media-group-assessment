export default function DashboardLayout({ children }) {
  // App shell is provided by `components/AppShell` (used by pages).
  // Keeping this layout minimal prevents rendering the sidebar/header twice.
  return children
}
